<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{t 'email.ghostmode-reminder.subject'}}</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background-color: #f0f0f0;
        }
        .container {
            background-color: #ffffff;
            border-radius: 10px;
            padding: 30px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
        }
        h2 {
            color: #ff4757;
            font-size: 28px;
            margin: 0;
        }
        .content {
            font-size: 16px;
        }
        .warning-icon {
            font-size: 48px;
            text-align: center;
            margin-bottom: 20px;
        }
        .cta-button {
            display: inline-block;
            background-color: #ff4757;
            color: white;
            padding: 12px 24px;
            text-decoration: none;
            border-radius: 5px;
            margin: 20px 0;
        }
        .footer {
            font-size: 12px;
            color: #888;
            text-align: center;
            margin-top: 30px;
            font-style: italic;
        }
    </style>
</head>
<body>
<div class="container">
    <div class="header">
        <div class="warning-icon">⚠️</div>
        <h2>{{t 'email.ghostmode-reminder.header'}}</h2>
    </div>
    <div class="content">
        <p>{{t 'email.ghostmode-reminder.greeting' firstName=firstName}},</p>
        <p>{{t 'email.ghostmode-reminder.timeInGhost' hours=hours}}</p>
        <p>{{t 'email.ghostmode-reminder.impact'}}</p>
        <p>{{t 'email.ghostmode-reminder.suggestion'}}</p>
    </div>
    <div class="footer">
        <p>{{t 'email.ghostmode-reminder.footer'}}</p>
    </div>
</div>
</body>
</html>