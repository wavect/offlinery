<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email Notification Settings</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            max-width: 600px;
            margin: 0 auto;
            padding: 40px 20px;
            background-color: #f5f5f5;
            color: #333;
        }
        .container {
            background-color: white;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        h1 {
            color: #36797d;
            margin-bottom: 30px;
            text-align: center;
        }
        .form-group {
            margin-bottom: 20px;
        }
        .checkbox-wrapper {
            display: flex;
            align-items: center;
            margin-bottom: 15px;
        }
        .checkbox-wrapper input[type="checkbox"] {
            margin-right: 10px;
        }
        button {
            width: 100%;
            padding: 12px;
            font-size: 16px;
            background-color: #36797d;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        button:hover {
            background-color: #2c6164;
        }
        .message {
            margin-top: 15px;
            padding: 10px;
            border-radius: 4px;
        }
        .success { background-color: #dff0d8; color: #3c763d; }
        .error { background-color: #f2dede; color: #a94442; }
    </style>
</head>
<body>
<div class="container">
    <h1>Email Notification Settings</h1>
    <form id="settingsForm">
        {{#if loading}}
            <p>Loading settings...</p>
        {{else}}
            {{#each notifications}}
                <div class="checkbox-wrapper">
                    <input type="checkbox"
                           id="{{this.id}}"
                           name="{{this.id}}"
                           {{#if this.enabled}}checked{{/if}}>
                    <label for="{{this.id}}">{{this.name}}</label>
                </div>
            {{/each}}
            <button type="submit">Save Changes</button>
        {{/if}}
        {{#if message}}
            <div class="message {{messageType}}">
                {{message}}
            </div>
        {{/if}}
    </form>
</div>

<script nonce="{{nonce}}">
    async function fetchSettings() {
        try {
            const response = await fetch('/v1/user/get-notification-settings', {
                headers: {
                    "authorization": `Bearer {{jwtToken}}`
                }
            });
            if (!response.ok) throw new Error('Failed to fetch settings');
            return await response.json();
        } catch (error) {
            console.error('Error:', error);
            return [];
        }
    }

    document.getElementById('settingsForm').addEventListener('submit', async (e) => {
        e.preventDefault();
        const settings = Array.from(document.querySelectorAll('input[type="checkbox"]'))
            .map(checkbox => ({
                id: checkbox.id,
                enabled: checkbox.checked
            }));

        try {
            const response = await fetch('/v1/user/update-notification-settings', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                    'Authorization': 'Bearer {{jwtToken}}'
                },
                body: JSON.stringify({ settings })
            });

            if (!response.ok) throw new Error('Failed to update settings');

            const messageDiv = document.createElement('div');
            messageDiv.className = 'message success';
            messageDiv.textContent = 'Settings updated successfully';
            document.getElementById('settingsForm').appendChild(messageDiv);
        } catch (error) {
            const messageDiv = document.createElement('div');
            messageDiv.className = 'message error';
            messageDiv.textContent = 'Failed to update settings';
            document.getElementById('settingsForm').appendChild(messageDiv);
        }
    });
</script>
</body>
</html>